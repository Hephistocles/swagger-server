// NOTE: This is an invalid swagger spec. 
// It demonstrates the set of members which are currently used in swagger-server and thus omits
// members which are part of the swagger specification which are not implemented in swagger-server

{
    "swagger": "2",
    "basePath": "/pet",
    "paths": {
        "/": {
            "get": {
                "operationId" : "getAllPets"
            },
            "post": {
                "operationId": "createPet",
                "parameters": [
                    {
                        "name": "pet",
                        "in": "body"
                    }
                ]
            }
        },
        "/{petId}": {
            "get": {
                "operationId": "changePetName",
                "parameters": [
                    {
                        "name": "petId",
                        "in": "path"
                    },
                    { 
                        "name": "petName",
                        "in":"body"
                    }
                ]
            }
        }
    }
}
